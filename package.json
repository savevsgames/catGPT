{
  "name": "tomogatch.ai",
  "version": "1.0.0",
  "description": "TODO!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm i && cd ../server && npm i",
    "server": "cd server && npm run start",
    "client:build": "cd client && npm run build",
    "client:dev": "cd client && npm run dev",
    "render:build": "npm install && cd client && npm run build && cd ../server && npm run build",
    "render:start": "npm run render:build && npm run server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^22.7.6",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.24.0"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "moment": "^2.30.1",
    "web-vitals": "^4.2.3"
  }
}

{
  "name": "tomogatch.ai",
  "version": "1.0.0",
  "description": "TODO!",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd client && npm i && cd ../server && npm i",
    "server" : "cd server && npm run start",
    "client:build": "cd client && npm run build",
    "client:dev" : "cd client && npm run dev",
    "render:build": "npm i && cd client && npm run build && cd ../server && npm run build",
    "render:start": "npm run client:build && npm run server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.7.6"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.24.0"
  },
  "dependencies": {
    "web-vitals": "^4.2.3"
  }
}
